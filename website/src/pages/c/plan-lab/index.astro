---
import Site from '../../../layouts/Site.astro';
import { VariantStoryDesk, VariantStoryWell, VariantStoryOneDesk } from '../../../components/demo/PlanLab.tsx';
---
<Site noindex>
  <div class="plan-lab">
    <header class="plan-lab-head">
      <h1>Plan section lab</h1>
      <p>Story rows in containers: desktop cards, inset wells, one shared desktop.</p>
    </header>

    <section class="plan-lab-concept" id="v3d">
      <div class="plan-lab-tag">3d · Desktop cards — each mock on its own wallpaper backdrop, hue per row</div>
      <VariantStoryDesk client:visible />
    </section>

    <section class="plan-lab-concept" id="v3e">
      <div class="plan-lab-tag">3e · Inset wells — recessed containers, mocks cropped at the edge</div>
      <VariantStoryWell client:visible />
    </section>

    <section class="plan-lab-concept" id="v3f">
      <div class="plan-lab-tag">3f · One shared desktop — the whole stage on a single wallpaper container</div>
      <VariantStoryOneDesk client:visible />
    </section>
  </div>

  <style is:global>
    .plan-lab {
      max-width: 1200px;
      margin: 0 auto;
      padding: 64px var(--content-padding) 200px;
    }
    .plan-lab-head p {
      color: #86868b;
      margin: 8px 0 0;
    }
    .plan-lab-concept {
      margin-top: 160px;
      padding-top: 40px;
      border-top: 1px solid rgba(255, 255, 255, 0.08);
    }
    .plan-lab-tag {
      font-family: 'Iosevka Extended', 'SF Mono', monospace;
      font-size: 13px;
      color: #86868b;
      margin-bottom: 56px;
    }

    /* Shared marketing framing */
    .plan-v-headline {
      font-size: 44px;
      text-align: center;
      margin: 0;
    }
    .plan-v-sub {
      color: #86868b;
      font-size: 17px;
      text-align: center;
      max-width: 560px;
      margin: 14px auto 0;
      line-height: 1.5;
    }

    /* Story rows */
    .plan-v3-row {
      display: flex;
      align-items: center;
      gap: 40px;
    }
    .plan-v3-copy {
      width: 240px;
      flex-shrink: 0;
    }
    .plan-v3-copy h3 {
      font-size: 21px;
      margin: 0;
    }
    .plan-v3-copy p {
      color: #86868b;
      font-size: 15px;
      line-height: 1.5;
      margin: 10px 0 0;
    }
    .plan-v3-mock {
      flex: 1;
      min-width: 0;
    }

    /* Containers separate from the surfaces */
    .plan-desk {
      border-radius: 24px;
      border: 1px solid rgba(255, 255, 255, 0.07);
      padding: 40px;
      /* clip, not hidden: hidden makes the desk a scroll container, which
         pins the shared-desktop variant's sticky column in place. */
      overflow: clip;
      position: relative;
    }
    .plan-desk > .glass-bevel {
      box-shadow:
        var(--shadow-panel),
        0 32px 64px -32px rgba(0, 0, 0, 0.75);
    }
    .plan-well {
      border-radius: 24px;
      border: 1px solid rgba(255, 255, 255, 0.05);
      background: color-mix(in srgb, var(--color-ink) 4%, transparent);
      box-shadow: inset 0 1px 0 rgba(0, 0, 0, 0.4);
      padding: 36px;
      overflow: hidden;
      position: relative;
    }
    /* Choreography */
    .plan-lab-card-in {
      animation: plan-lab-card-in 320ms ease-out;
    }
    @keyframes plan-lab-card-in {
      from {
        opacity: 0;
        transform: translateY(-8px);
      }
      to {
        opacity: 1;
        transform: none;
      }
    }
    .plan-lab-clearing {
      opacity: 0;
      transition: opacity 350ms ease-out;
    }
    .plan-lab-firing {
      box-shadow:
        var(--shadow-panel),
        0 0 0 1px color-mix(in srgb, var(--color-accent) 55%, transparent),
        0 0 24px color-mix(in srgb, var(--color-accent) 18%, transparent) !important;
      transition: box-shadow 300ms ease-out;
    }
  </style>
</Site>
